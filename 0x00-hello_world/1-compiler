#!/bin/bash

CFILE="main.c"        # Update this variable with your C file name

# Extract the base name of the C file without the extension
BASENAME="${CFILE%.*}"

# Compile the C file without linking
gcc -c "$CFILE" -o "$BASENAME.o"

echo "Compilation completed. Object file saved as $BASENAME.o"
